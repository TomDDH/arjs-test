import React, { useEffect } from "react";
import { Box } from '@mui/material';


const RedirectCard = (props) => {

  return (
    <React.Fragment>
      <Box sx={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        top: '0',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        zIndex: '600',
        backgroundColor: '#242424d1',
      }}>
        <div class="klaviyo-form-X6t5js"></div>
      </Box>

    </React.Fragment >
  )
}
export { RedirectCard }
